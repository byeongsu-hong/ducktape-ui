[package]
name = "wasm-view-frame"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
bincode = "1.3"

[lints.rust]
unsafe_code = "forbid"
