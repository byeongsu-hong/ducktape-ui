# Tree flex layouts

Continue module-view phase 4 after keyed/virtual and lazy (#969 in CI). Ducktape
remains read-only. Source audit of Ducktape 06e0378b4 (now under
`/home/eddy/dev/ducktape/ducktape`) finds five flex sites: reaction chips, two
reaction pickers, Forge composer/send and Explorer filters. They need wrapping,
independent gaps, start/end alignment, padding and fill/fixed widths.

## Design

Copy flex layout and per-item options into wire data and use the existing native
`flex`/`flex_item` host renderer. Guests perform no measurement or layout callback.
Preserve all currently checked flex options: direction/reversal, wrapping,
justification and alignment, gaps, padding, dimensions/maxima, clip, min-cell;
item order/grow/shrink/basis/align-self/margins. Reuse the native generator's
control-flow expansion so if/for/match items and component scopes remain aligned.

Wire Flex has parallel item-options and child vectors, like KeyedColumn's keys
and children. Decode metadata with a count limit, sanitize finite numeric values,
and normalize metadata to the surviving child count. Missing options use native
defaults; excess metadata is discarded. Child data uses existing recursive
bounds/traversal. Styles resolve in the guest and remain host-painted.

## Work and evidence

1. Add bounded wire types/traversal/sanitization with malformed-frame tests.
2. Emit flex and item metadata from the checked lowering; compile all options,
   conditional/loop/match children and generated Rust in an actual wasm bundle.
3. Render through the native engine. Compare host geometry/routes with native
   flex for wrap/reversal, grow/shrink, margins/basis and clipping; use an
   intended behavior mutation to prove each new regression assertion.
4. Actual wasm fixture models reaction buttons and a fill composer beside Send;
   click after width changes and assert resulting guest state. Check flow order
   and wrap geometry independently of generated-code string checks.
5. Update SPEC/README/COVERAGE/app-store status and CI. Rebase onto merged lazy
   with workspace --tests check before continuing. Run relevant tests, lint,
   formats and actual bundle, review full diff, PR, green CI, merge and cleanup.

## Current implementation

Worktree rebased to formatting main 3088c1b1 with workspace --tests check.
Wire Flex and bounded item metadata, shared control-flow expansion, full option
lowering, host native engine mapping and actual wasm fixture are implemented.
Native tests cover wrap/reflow, independent gaps, item order/grow/percentage
basis and outer utility sizing. Actual wasm pointer clicks select loop/component
reaction buttons after resizing, then native keyboard input and Send update the
guest state. Full flex options also compile in the bundled fixture.

Review found an outer-sizing parity gap. Utility dimensions/max-width are now
copied separately from explicit inner dimensions and applied to the existing
painted container; no additional layout engine or wrapper was introduced.
Both host/wire and codegen reviewers have no remaining findings at this point.

## Evidence so far

- Unbounded numeric rules fail NaN versus zero; restored finite bounds preserve
  legitimate negative margins (.flex-rules-red.log, .flex-wire-green.log).
  Initial infinity expectations were corrected to match existing wire clamping.
- Disabling host wrap/order fails expected child positions; restoration passes
  (.flex-host-red.log, .flex-host-restored.log).
- Omitting outer sizing produces width100 instead of180; restored all3 native
  flex tests pass (.flex-surface-size-{red,green}.log).
- Removing wrap from the actual guest fails the intended narrow-row assertion
  (.flex-wasm-wrap-red.log). Earlier indentation setup failure does not count.
- Removing metadata normalization fails item cardinality; bypassing the prefix
  cap fails the assertion that oversized metadata is rejected before attempting
  payload reads (.flex-wire-bounds-red.log). Production code is restored.
- Complete Tree option/control-flow codegen test passes. Multiple utility tokens
  need separate with lines; earlier invalid fixture syntax is not Red evidence.

Remaining: verify restored bundle/tests, rebase onto lazy main e133d48d with
workspace --tests check before continuing, integrate flex/lazy traversal, update
public support docs and CI, run combined local checks, final review/PR/green CI.
Do not claim the full phase or Ducktape port complete from this implementation.
